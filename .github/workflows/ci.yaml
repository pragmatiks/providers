name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "0.5.x"

      - name: Set up Python
        run: uv python install 3.13

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Lint
        run: uv run ruff check .

      - name: Format check
        run: uv run ruff format --check .

      - name: Test GCP provider
        working-directory: packages/gcp
        run: uv run pytest tests/ --cov=gcp_provider --cov-report=term-missing

      - name: Test Anthropic provider
        working-directory: packages/anthropic
        run: uv run pytest tests/ --cov=anthropic_provider --cov-report=term-missing

      - name: Test OpenAI provider
        working-directory: packages/openai
        run: uv run pytest tests/ --cov=openai_provider --cov-report=term-missing
