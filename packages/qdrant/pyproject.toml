[project]
name = "pragmatiks-qdrant-provider"
version = "0.11.0"
description = "Qdrant provider for Pragmatiks"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pragmatiks-sdk>=0.6.0",
    "pragmatiks-kubernetes-provider>=0.2.0",
    "qdrant-client>=1.12.0",
]

authors = [
    { name = "Pragmatiks", email = "team@pragmatiks.io" },
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-asyncio>=1.0.0",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.14.0",
    "ty>=0.0.14",
]

[tool.pragma]
# Provider metadata - used by the platform for discovery and deployment
provider = "qdrant"
package = "qdrant_provider"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[tool.commitizen]
name = "cz_conventional_commits"
version = "0.11.0"
version_files = ["pyproject.toml:^version"]
tag_format = "qdrant-v$version"
update_changelog_on_bump = true
changelog_file = "CHANGELOG.md"

[tool.ruff]
extend = "../../pyproject.toml"
line-length = 120

[tool.uv.build-backend]
module-name = "qdrant_provider"

[build-system]
requires = ["uv_build>=0.9.8,<0.10.0"]
build-backend = "uv_build"
